services:
  brownfield-backend:
    image: ghcr.io/astral-sh/uv:python3.12-bookworm-slim
    working_dir: /app
    volumes:
      - ./brownfield-backend:/app:z
      - uv-cache:/root/.cache/uv:z
    command: uv run uvicorn petclinic.main:app --reload --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

  brownfield-frontend:
    image: node:22
    working_dir: /app
    volumes:
      - ./brownfield-frontend:/app:z
      - /app/node_modules
    command: ["/bin/sh", "-c", "npm install && npm run dev -- --host 0.0.0.0"]
    ports:
      - "5173:5173"
    environment:
      - CHOKIDAR_USEPOLLING=true

volumes:
  uv-cache:

networks:
  default:
    driver: bridge
